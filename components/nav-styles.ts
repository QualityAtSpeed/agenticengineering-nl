// Shared surface for the floating nav and its mobile panel. Full class strings
// (not built at runtime) so Tailwind's source scanner picks them up.
export const ISLAND =
  'rounded-2xl border border-border-subtle bg-bg-base/95 backdrop-blur shadow-[0_8px_24px_-12px_rgb(0_0_0/0.35)]';
